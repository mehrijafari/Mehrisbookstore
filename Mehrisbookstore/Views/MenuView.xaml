<UserControl x:Class="Mehrisbookstore.Views.MenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Mehrisbookstore.Views" xmlns:fa="http://schemas.awesome.incremented/wpf/xaml/fontawesome.sharp"
             xmlns:UI="clr-namespace:Mehrisbookstore.ViewModel"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=UI:MainWindowViewModel,IsDesignTimeCreatable=True}"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Menu>
            <MenuItem Header="_Store">
                <MenuItem Header="_Edit quantity"
                          Command="{Binding StoresViewModel.OpenEditQuantityCommand}"/>
                <MenuItem Header="_Transfer books"/>
            </MenuItem>
            <MenuItem Header="_Author">
            </MenuItem>
            <MenuItem Header="_Titles">
            </MenuItem>
            <MenuItem Header="_Books" Icon="{fa:Icon book}">
            </MenuItem>
            <MenuItem Header="_Publishers">
                
            </MenuItem>
        </Menu>
    </Grid>
</UserControl>
